name: Build (client-panel)

on:
  pull_request:
    paths:
      - ".github/workflows/**"
      - "apps/client-panel/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "package.json"
      - "turbo.json"
  push:
    branches: [main]
    paths:
      - ".github/workflows/**"
      - "apps/client-panel/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "package.json"
      - "turbo.json"

jobs:
  build-client:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: pnpm/action-setup@v5
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter client-panel build
